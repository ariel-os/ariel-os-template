imports:
  - git:
      url: https://github.com/ariel-os/ariel-os
      tag: v0.3.0
    dldir: ariel-os

apps:
  - name: {{ project-name }}
{% if crate_type == "lib" %}
    selects:
      - embedded-test-only
{% endif -%}
